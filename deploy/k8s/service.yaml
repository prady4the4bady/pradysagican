apiVersion: v1
kind: Service
metadata:
  name: pradysagican-api
  labels:
    app: pradysagican-api
spec:
  selector:
    app: pradysagican-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

